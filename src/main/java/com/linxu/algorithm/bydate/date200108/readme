最长不重复字符字符串算法

问题：查找最长不包含重复字符的子字符串


解决思路：
1、暴力解：O(N^3)
2、动态规划解O(N)
    解决思路：第I个字符为结尾的子字符串，其最大的不重复子字符串长度为f(i)
                如果I未出现重复，即有f(i)=f(i-1)+1;
                如果I出现重复，求出i-pre_i=d;
                    如果d>f(i-1);则证明出现重复是在i-1最大子字符串之前，则f(i)=f(i-1)+1;
                    如果d<=f(i-1);则证明在f(i-1)中出现重复，则最大长度f(i)=d;

个人问题：
1、动态规划的分析。

时间：