##辅助栈的使用


实现栈最小数函数算法

问题：
实现将数据往栈压入，同时实现一个min方法，该方法能够获取到该栈的最小值。


解决思路：
1、使用两个栈，一个是数据栈，一个是辅助栈；
2、假设数据栈压入为  3 4 2 1 则辅助栈压入为3 3 2 1，即在压入数据栈的时候，获取辅助栈的元素，如果比辅助栈的小，则压入；如果大，
则再压入一个与辅助栈的顶元素相同的元素。

个人问题：

输入两个序列，一个是入栈序列，一个是出栈序列，判断这两个序列是否匹配的算法

解决思路：
1、使用一个辅助栈，用于把入栈的元素压入栈中，直到压入到出栈序列的头部（同样压入）。
比如：1、2、3、4、5；4、5、3、2、1； 即压入到1、2、3、4；
2、然后执行出栈，栈顶元素与出栈指针所指的元素比较，相同则弹出，不同的话，如果还有元素没有入栈，则重复步骤1.
3、如果全部入栈了，但是还是未能够匹配，则不匹配，否则栈弹出到空，则为true.


时间：2019/10/17