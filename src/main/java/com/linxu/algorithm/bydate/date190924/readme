大数运算算法

问题：
实现两个大数整数的加法，包含负数。


解决思路：
1、采用计算机的底层思想，将0代表正，9代表负。
2、将两个输入的位数纠正为一致，用0表示正，用9表示负。则1的反码为8
                            //[符号][0]...[X]
                            做加法运算。得出结果，再求补码（记得加1），即补码的补码是原码。这个时候就是真正的数值了。
 负数的补码等于除符号位之外的原码的反码+1；

个人问题：
1、机器码的运算规则
2、大数处理思想

时间：2019/09/24