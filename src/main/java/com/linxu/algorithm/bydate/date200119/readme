求两条单链表的第一个公共节点算法

问题：
输入两个链表，找出它们的第一个公共结点。

解决思路：
1、使用两个辅助栈，遍历两个链表将节点入栈，然后出栈，找到相等的节点（最后一个相等节点）
2、使用计步器，计算两个链表的长度差值，然后长的先走N步，然后同时走，直到相等即返回。

个人问题：

问题：在排序数组中查找某一个数字，并返回该数字出现的次数的算法

解决思路：
1、利用二分查找，找到第一个索引，找到第二个索引，总的复杂度为LogN，然后索引相减即可


时间：2020/1/19