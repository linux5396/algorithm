递增数组序列算法

问题：
1、求任意一对和为S的序列，利用快排思想，高低查找。
2、在1~N的数字中，查找和为N的序列，利用small、big指针，当和大于sum，则small指针向前走；
当和小于sum,则big指针向前走。


解决思路：
1、求任意一对和为S的序列，利用快排思想，高低查找。
2、在1~N的数字中，查找和为N的序列，利用small、big指针，当和大于sum，则small指针向前走；
当和小于sum,则big指针向前走。

个人问题：


时间：