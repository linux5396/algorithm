数据流的中位数问题算法

问题：
数据流的中位数求取



解决思路：
1、使用数组排序（最快NlogN）
2、使用AVL，由于平衡的原因，中位数为ROOT，但是插入需要LogN
2、大顶堆和小顶堆，实现容易，且为LogN

个人问题：
1、采用优先队列进行实现（实际也是大小顶堆）。
2、采用自实现的大小顶堆进行实现
3、大顶堆为左，小顶堆为右  。偶数个则大小顶堆的顶堆元素相加>>1 ;奇数个则取小顶堆的堆顶元素。

时间：2019/11/11