字符的全排列算法

问题：
1、实现对字符进行全排列并输出


解决思路：
1、递归：（固定第一个元素，将剩下的元素进行全排列；（那剩下的元素中又可以分为第一个和剩下的元素，这样就用到了递归））
2、每一个字符都要有做第一个元素的机会，即将剩下的元素依次与第一个进行交换
3、每次交换后的数据，在进行递归处理了之后需要再交换回原来的位置，以便后面的字符与其在进行交换

个人问题：
1、递归

问题：
1、实现对数字字符如42531，查找该值大的最小值。

解决思路：
1、从尾部开始找，找到第一个升序序列的IDX
2、利用栈存储最小最接近元素
3、从IDX到END开始实现全排列，找到最接近的最小数。

时间：2019/10/30  2019/11/07