栈排序算法算法

问题：
在不使用额外的数据结构下，利用一个栈对一个栈进行排序。

解决思路：
1、如果CUR小于等于HELP栈顶元素，那么压入HELP
2、如果CUR大小HELP栈顶元素，那么弹出HELP，直到HELP为空或者CUR小于等于HELP栈顶元素，再将CUR压入HELP
3、重复以上步骤，直到DATASTACK为空，那么将HELPSTACK中的元素反压到DATASTACK中即可。

个人问题：


时间：2019/12/05