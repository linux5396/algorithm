数组策略重排序算法

问题：
1、一个整型数组，要求将奇数排在前半部分，偶数排在后半部分。

解决思路：
1、与快排的思想一致，使用两个指针，low与high，从两端可以交换位置不正确的元素，直到整个数组交换完毕即可。

个人问题：
1、拓展性问题（假如要求改成策略型的，即可拓展（负数在前，其余在后等情况）
    那么在java中是没函数指针这个说法的，因此可以改成抽象类、接口、反射）

时间：2019/10/03