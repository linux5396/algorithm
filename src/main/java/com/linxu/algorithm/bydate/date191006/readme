单链表倒数第K个结点算法

问题：
使用一次遍历，找出链表的倒数第K个结点并且返回。


解决思路：
1、使用双指针，指针间隔为K-1.
2、在移动指针的时候，要记得处理特殊情况：
    a.刚好是尾结点
    b.长度不足的处理

个人问题：
1、拓展，使用双指针判断是否存在链表环。
2、双指针找到链表的中间结点：
    a.都从第一个开始，一个走两步，一个走一步，直到走两步的走完，一步的位置就是中间结点。

求单链表中环的入口

问题：求单链表是否存在环，若存在，就该环的入口

解决思路：
1、双指针法，判断是否存在环
2、使用计数器，计算环的大小
3、重新定义双指针，之间的距离为环的大小，当恰好相遇，则是环的入口

时间：2019/10/06