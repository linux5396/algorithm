生成窗口的最大值算法

问题：
 * 生成窗口的最大值数组：
 * 输入一个ARRAY，以及窗口的大小，窗口每次滑动一格，输出每次窗口内的最大值。

解决思路：
1、O(N*M)直接遍历，每次都进行一次窗口内的寻找
2、O(2N)，直接遍历，外加双向队列，满足队列中的基本下标对应的元素数据为大->小，同时注意窗口过期问题。

个人问题：
1、双向队列存放索引的使用，而非直接存放值

时间：2019/12/06