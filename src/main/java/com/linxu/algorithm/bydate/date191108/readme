数组中次数超过一半的数字的算法

问题：
输入一个数字数组，找出其中出现次数超过一半的数字，并返回输出。


解决思路：
1、hashMap解决
2、观察特点，设置两个变量，即key、value，如果key==nextKey,value++,or value--;遍历一遍，找到value>0的最后一个key,同时进行
O(N)的遍历计算检查（如果没这个，会因为数字顺序的排列情况导致结果错误）

个人问题：

时间：2019/11/08