位运算算法

问题：
1、在数组中有两个数字出现了1次，其余都出现了2次，请在O(N)的时间复杂度与O(1)的空间复杂度内找出这两个数字
2、一个数字中一个数字出现了1次，其余数字出现了三次，找出这个数字。


解决思路：
1、进行一次异或，然后取异或结果的某一个位是1的，进行分组，再分组异或即可。
2、利用一个32长度的数组，把整个数组32位都加上，如果结果某一位能给3整除，则这位是0；否则为1；然后再把32
位的数组的位结果存放到int中进行返回即可

个人问题：
1、掌握位运算

时间：