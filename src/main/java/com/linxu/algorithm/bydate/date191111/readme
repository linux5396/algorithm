连续子数组的最大和算法

问题：
输入一个连续的整型数组，求其中的连续子数组的和的最大值。

解决思路：
1、利用动态规划：
     *     动态规划思想构造出如下的递归公式：
     *     f(i)表示当前的和.
     *   f(i)=
     *          array[i]; i=0 or f(i-1)<=0
     *          f(i-1)+array[i]; i!=0&&f(i-1)>0
     *          //返回整个过程中最大的和即可。
     *     return max(f(i));
     *


个人问题：
1、动态规划的算法思想的应用

时间：2019/11/11